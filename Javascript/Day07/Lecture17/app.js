console.log(x); //undefined
var x = 10;

console.log(z); //error
let z = 10


//hoisting
// Hoisting means JavaScript moves declarations (not assignments) to the top of their scope before code execution.

// Think of it like this:
// JS reads your code first, remembers all the declared variables and functions, and then runs the code line by line.